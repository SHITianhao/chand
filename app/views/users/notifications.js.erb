$('#feed').append("<%= j render @notifications %>");

<% if @notifications.next_page %>
  $(".pagination").replaceWith("<%= j will_paginate(@notifications) %>");
  $(".pagination").hide();
<% else %>
  $(window).off('scroll');
  $("#loader").remove();
  $(".pagination").remove();
<% end %>